{
  "name": "Automatically Telegram Escrow Bot",
  "description": "A Telegram Escrow Bot + Pyrogram Userbot that auto-creates private escrow groups.",
  "repository": "https://github.com/GolgiBody1/Automatically-bot",
  "keywords": ["telegram", "bot", "pyrogram", "escrow", "python", "railway", "heroku"],
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "env": {
    "BOT_TOKEN": {
      "description": "Your Telegram Bot Token from @BotFather",
      "required": true
    },
    "API_ID": {
      "description": "Your Telegram API ID from https://my.telegram.org/apps",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API Hash from https://my.telegram.org/apps",
      "required": true
    },
    "SESSION": {
      "description": "Your Pyrogram string session (to run the userbot)",
      "required": true
    }
  },
  "scripts": {
    "postdeploy": "echo 'Escrow Bot deployed successfully!'"
  }
}
